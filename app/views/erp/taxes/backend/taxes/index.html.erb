<% content_for :title do %>
    <%= t('.taxes') %>
<% end %>

<% content_for :page_title do %>
    <i class="fa fa-file-text-o"></i> <%= t('.taxes') %>
<% end %>

<% content_for :page_toolbar do %>
    <%= erp_component('button/create', {
        text: t('.create'),
        href: erp_taxes.new_backend_tax_path,
        class: 'btn-sm'
    }) %>
<% end %>

<%= erp_datalist(
    url: erp_taxes.list_backend_taxes_path,
    keywords: [
        {
            text: t('.name'), name: 'name'
        }
    ],
    actions: [
        {
            text: '<i class="fa fa-eye-slash"></i> '+t('.archive'),
            url: erp_taxes.archive_all_backend_taxes_path,
            data_method: 'PUT'
        },
        {
            text: '<i class="fa fa-eye"></i> '+t('.unarchive'),
            url: erp_taxes.unarchive_all_backend_taxes_path,
            data_method: 'PUT'
        },
        {
            divider: true
        },
        {
            text: '<i class="fa fa-trash"></i> '+t('.delete'),
            url: erp_taxes.delete_all_backend_taxes_path,
            data_method: 'DELETE',
            data_confirm: t('.delete_confirm')
        }
    ],
    columns: [
        {
            text: t('.name'), name: 'name', value: true, checked: true
        },
        {
            text: t('.status'), name: 'status', value: true, checked: true
        },
        {
            text: t('.creator'), name: 'creator', value: true, checked: true
        },
        {
            text: t('.created_at'), name: 'created_at', value: true, checked: true
        }
    ]
) %>    